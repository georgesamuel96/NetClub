<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyAppTheme" parent="@style/AppTheme" />

    <style name="Theme.App.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/word_gray</item>
        <item name="colorControlActivated">@color/word_gray</item>
        <item name="colorControlHighlight">@color/word_gray</item>
    </style>

    <style name="custom_editText" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/word</item>
        <item name="colorControlActivated">@color/word</item>
        <item name="colorControlHighlight">@color/word</item>
    </style>

</resources>
